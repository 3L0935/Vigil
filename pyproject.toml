[project]
name = "vigil"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "faster-whisper>=1.0.0",
    "sounddevice>=0.4.6",
    "numpy>=1.24.0",
    "scipy>=1.11.0",
    "customtkinter>=5.2.0",
    "pynput>=1.7.6",
    "Pillow>=10.0.0",
    "httpx>=0.27.0",
    "ddgs>=9.0",
    "pyperclip>=1.8.2",
    "dbus-python>=1.3.2",
    "PyGObject>=3.42.0",
    "huggingface-hub>=0.23.0",
    "PySide6>=6.8,<7",
    "jeepney>=0.8.0",
]

[project.scripts]
vigil = "main:main"
vigil-trigger = "vigil_trigger:main"

[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

# Flat layout with package directories and an `img/` asset dir confuses
# setuptools auto-discovery ("Multiple top-level packages discovered").
# Pin the included packages + explicitly list top-level modules so
# `uv sync` can always rebuild the editable install.
[tool.setuptools]
py-modules = [
    "main", "app_launcher", "assistant", "brand", "compositor", "config",
    "database", "first_run", "injector", "json_repair", "llm_backend",
    "llm_manager", "locales", "logger", "obsidian", "platform_linux",
    "recorder", "service", "settings_window", "setup_utils", "theme",
    "transcriber", "tray_qt", "tts", "vigil_trigger", "widget",
    "privacy", "data_paths", "dictation", "recovery", "clipboard_bridge",
    "dictation_settings", "url_shortcuts", "folders", "file_search",
]

[tool.setuptools.packages.find]
include = ["hotkey*", "vigil_ui*"]
exclude = ["img*", "tests*", "docs*"]

[tool.setuptools.package-data]
vigil_ui = ["qml/*.qml", "qml/components/*.qml"]

[project.optional-dependencies]
tts-piper = ["piper-tts"]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
